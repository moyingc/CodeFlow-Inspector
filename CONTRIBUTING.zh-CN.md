# 为 CodeFlow Inspector 贡献代码

CodeFlow Inspector 是一套本地优先的桌面代码分析系统。贡献内容必须保持语义事实、视觉表现、确定性知识、学习排序、受控执行与形式化证明之间的边界。

## 提交拉取请求之前

1. 创建范围明确的 Issue，并提供最小化、非机密的复现样例。
2. 不要提交导入的源代码、本地 SQLite 数据库、运行输出、sidecar 二进制文件、签名材料或用户路径。
3. 运行 `npm run lint` 和 `npm test`。
4. 修改 Rust 或原生命令后，在 `src-tauri` 中运行 `cargo test --locked`。
5. 修改可见文字后，检查 `zh-CN` 与 `en-US` 两种桌面布局。

## 分析完整性

- 不得仅为使图形更整洁而改变 Semantic Graph。
- 不得把启发式或估算结果标记为编译器、运行时或证明证据。
- 不得在视觉层合并彼此独立的语义通道。
- 除非用户明确启用白名单知识更新，否则必须拒绝网络访问。
- 导入的可执行适配器必须保持隔离，直至通过校验和、健康检查和隔离要求。

## 有效的反馈内容

请提供产品版本、操作系统、受影响语言、证据等级、准确步骤、预期结果、实际结果及最小样例项目。不要附加专有项目或秘密信息。

英文原文：[CONTRIBUTING.md](CONTRIBUTING.md)
